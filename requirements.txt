# ePustaka-Munshi - Library Management System
# Web/API dependencies (installed on Vercel).
# OCR/imaging processing deps are local-only -> requirements-ocr.txt
# (Vercel has no Tesseract/Poppler binaries and never runs OCR).

Flask==3.0.0
Flask-SQLAlchemy==3.1.1
Flask-Login==0.6.3
Flask-WTF==1.2.1
Flask-CORS==4.0.0
Werkzeug==3.0.1
SQLAlchemy==2.0.49
# psycopg (v3) ships wheels for current Python versions, so Vercel's uv
# installer never compiles from source (which needs pg_config/libpq).
psycopg[binary]>=3.2

# Pillow stays: python-barcode's ImageWriter renders barcode PNGs at runtime
Pillow==10.4.0

# Excel import/export
openpyxl==3.1.5

# Utilities
python-dotenv==1.0.0
python-dateutil==2.8.2
python-barcode==0.15.1
